# Changelog

## 0.0.20 - 2026-04-27

- 顶部状态增加未保存强调样式，只要有本地修改未写入 KV，就用更醒目的提示保留在页面顶部。
- 点击“完成”时如果仍有未保存修改，会明确提醒“完成只是退出编辑模式，不会保存到 Cloudflare KV”。
- 主题切换、导入、拖拽排序、删除、撤销、链接检测等本地修改统一提示“请记得保存到 Cloudflare KV”。

## 0.0.19 - 2026-04-27

- 导入预览弹窗改为固定底部确认区，合并导入、覆盖当前全部数据、取消和先导出当前数据按钮不再被预览内容挤到难找的位置。
- 导入预览内容区域独立滚动，手机端确认按钮会自动换行铺开，降低误操作和漏看确认入口的风险。

## 0.0.18 - 2026-04-27

- 编辑模式工具栏增加当前管理员密码保存方式，进入编辑后也能看到并切换“本次会话 / 记住此设备”。
- 旧浏览器里已经保存过管理员密码时，不再需要先清除密码才能找到保存方式入口。

## 0.0.17 - 2026-04-27

- 增加 Pages 版和 Chrome 插件版共享数据契约测试，覆盖扩展字段、iTab 导入和导入容量限制，确保两端 JSON 可以互相导入导出。
- KV 备份列表增加“下载 JSON”，恢复备份前显示备份时间、分组数、网站数，并明确恢复前会自动备份当前 KV 数据。
- 增加 `/api/health` 部署诊断和前台版本号显示，方便确认 KV binding、管理员密码和线上数据状态。
- 管理员密码保存方式改为“本次会话 / 记住此设备”，默认只保存在当前浏览器会话里，旧的本机记住密码仍兼容。
- 增加 `npm run smoke:direct`，自动检查 Direct Upload zip 的根目录资源结构，防止 JS/CSS 回到 `/assets/` 导致白屏。

## 0.0.16 - 2026-04-27

- 降低批量网址检测误报率：HEAD 异常会用轻量 GET 复核，只有 404/410 这类明确缺失才标记为疑似失效。
- 将超时、403/429、5xx 和网络失败归为“无法确认”，不再默认当作失效网站高亮。
- 网址维护面板区分“疑似失效”和“无法确认”，避免把 Cloudflare Worker 无法访问误解为浏览器无法打开。

## 0.0.15 - 2026-04-27

- 导入解析阶段同步 Cloudflare Worker 的容量限制，超过 500 个分组或单个分组超过 1000 个网站时会在预览前拒绝。
- 保留总计 5000 个网站和 10MB 文件限制，让超大导入不会等到保存 KV 时才失败。
- README 补充具体导入容量限制。

## 0.0.14 - 2026-04-27

- 重写 Pages 版 README，补齐当前已同步的 Chrome 版功能、数据互通说明和公网写入鉴权边界。
- 修正文档里的 Direct Upload zip 结构说明，明确 JS/CSS 必须位于 zip 根目录，不能回到 `/assets/`。
- 补充 KV binding、备份恢复、导入迁移和验证命令说明，降低后续部署和恢复误操作风险。

## 0.0.13 - 2026-04-27

- 强化导入覆盖保护，空导入或覆盖后网站数量减少时会先显示风险提示并要求二次确认。
- 导入预览增加当前网站数、覆盖后网站数和覆盖将减少数量，避免误把空文件保存到 Cloudflare KV。
- 备份入口改为“备份/恢复”，数据健康面板和 KV 备份面板增加“恢复最近非空备份”快捷入口。
- Direct Upload 构建结构继续保持 JS/CSS 在 zip 根目录。

## 0.0.12 - 2026-04-27

- 修复 favicon 加载失败时网站标题被挤到图标列、只显示少数字符的问题。
- 网站标题改为独立卡片样式，长标题最多显示两行，紧凑和列表布局保持稳定。
- Direct Upload 构建结构继续保持 JS/CSS 在 zip 根目录。

## 0.0.11 - 2026-04-27

- 同步 Chrome 版拖拽排序的误点击保护，排序松手后会短时间屏蔽同一卡片的点击，避免拖拽结束时意外打开网站。
- 收紧 Pages 版拖拽排序条件，只允许在管理员编辑模式、当前分组范围内排序；变更仍保留为本地未保存状态，等待手动保存到 Cloudflare KV。
- 同步搜索框键盘体验：`Esc` 清空搜索，非编辑模式下 `Enter` 打开第一个匹配网站，并沿用现有点击统计逻辑。
- Direct Upload 构建结构继续保持 JS/CSS 在 zip 根目录，公网写入仍由 `ADMIN_TOKEN` 保护。

## 0.0.10 - 2026-04-27

- 同步 Chrome 版的最近一次操作撤销入口，编辑、导入、排序、删除、链接检测等高风险修改可以先回滚本地状态，再决定是否保存到 Cloudflare KV。
- 增加网站卡片多选和批量工具条，支持把选中的网站移动到其它分组、批量清空自定义图标、取消选择或删除。
- 增加数据健康面板，集中显示分组数、网站数、重复项、链接检测状态数量和本地数据体积估算。
- 升级网址维护面板，增加检测进度、结果筛选、正常/受限/疑似失效统计，并保留定位、编辑、删除、打开和确认没问题操作。
- Direct Upload 构建结构继续保持 JS/CSS 在 zip 根目录，公网写入仍由 `ADMIN_TOKEN` 保护。

## 0.0.9 - 2026-04-27

- 增加管理员保护的 `POST /api/link-check` 批量检测接口，每批最多检测 50 个 `http/https` 网址，不写入 KV，只返回 `ok`、`limited`、`broken` 检测结果。
- 编辑模式新增“检测网址”入口，会把检测结果写入现有 `link.check` 字段并复用问题链接面板；结果仍需手动点击保存后才写入 Cloudflare KV。
- 检测逻辑对 401、403、405、429 和网络失败保持保守，标记为“受限”而不是自动删除，避免一次检测误判造成数据损失。

## 0.0.8 - 2026-04-27

- 增加 Cloudflare KV 备份列表 API，管理员鉴权后可查看最近备份的时间、分组数和网站数。
- 增加受保护的备份恢复 API；恢复前会先把当前 `dashboard` 自动保存为新的 `backup:<timestamp>`，再写入选中的备份。
- 编辑模式新增“备份”面板入口，可以读取备份列表并恢复到指定备份；恢复和保存一样继续要求管理员密码，不开放公网无鉴权写入。

## 0.0.7 - 2026-04-27

- 导入 JSON 或 iTab `.itabdata` 后先显示预览，不再立刻覆盖当前导航数据。
- 导入预览显示导入分组数、网站数、重复网址数、合并后网站数和跳过地址数，并提供“先导出当前数据”入口。
- 增加“合并导入”和“覆盖当前全部数据”两种确认方式；合并会跳过已有重复网址，所有导入变更仍需管理员保存后才写入 Cloudflare KV。

## 0.0.6 - 2026-04-27

- 同步 Chrome 插件版的链接健康维护入口，编辑模式下集中显示已记录为疑似失效或受限的链接。
- 问题链接支持定位、编辑、删除、打开和“确认没问题”，确认后只在本地标记为正常，仍需管理员保存后才写入 Cloudflare KV。
- 疑似问题的网站卡片会在当前分组里高亮，并用小标签标出状态，方便先人工确认，避免一次检测误判就删除链接。

## 0.0.5 - 2026-04-27

- 同步 Chrome 插件版的重复网址维护面板，编辑模式下会集中显示重复网址和所在分组/网站。
- 重复网站卡片会在当前分组中高亮，维护面板支持定位、编辑、删除、打开单条重复网址。
- 增加“保留第一个，删除其它”的批量整理入口，变更仍需管理员手动保存后才写入 Cloudflare KV。

## 0.0.4 - 2026-04-27

- 同步 Chrome 插件版的搜索范围控制，支持在当前分组和全部分组之间切换搜索。
- 全部分组搜索时，结果卡片会显示来源分组名，点击统计也会更新到原始分组里的对应网站。
- 编辑模式下搜索范围选择保持禁用，继续只编辑当前分组，避免跨分组拖拽和新增时混淆。

## 0.0.3 - 2026-04-27

- 同步前台深色/浅色切换按钮，切换后保留为未保存修改，仍需管理员保存才写入 Cloudflare KV。
- 增加编辑模式下的外观面板，支持卡片布局、壁纸预设、背景强度和当前分组颜色设置。
- 在页面根节点写入 `cardLayout`、`wallpaper` 和 `wallpaperIntensity` 数据属性，使 Cloudflare Pages 版可识别 Chrome 版导出的外观字段。
- 增加分组颜色样式、壁纸背景样式和紧凑/列表卡片布局样式，保持两端导入导出数据结构通用。

## 0.0.2 - 2026-04-27

- 同步编辑模式下的前台快捷操作：分组和网站卡片 hover 后显示编辑/删除小按钮。
- 增加左侧分组底部 `+` 新增入口，以及当前分组网站网格内的 `+` 新增网站卡片。
- 增加轻量弹窗式新增/编辑体验，替换原来占满卡片区域的内联编辑表单。
- 增加网站点击次数显示，普通浏览点击后只在本地状态累计，仍需管理员保存才写入 Cloudflare KV。
- 增加编辑模式下的网站卡片拖拽排序，排序结果同样保留为未保存修改，等待用户手动保存。

## 0.0.1 - 2026-04-27

- 同步 Chrome 插件版的数据结构扩展，支持点击统计、链接检测状态、分组颜色、卡片布局和壁纸设置。
- 增加通用导入解析能力，支持本程序 JSON 和 iTab `.itabdata`，并限制导入文件大小与链接数量。
- 增加重复网址、导入预览、合并导入、批量删除重复项和备份摘要等维护数据 helper。
- 强化 `sanitizeDashboard`，导入时自动补默认值并重建重复或缺失的分组/链接 ID。
- 更新 Cloudflare Pages Worker 保存校验，使扩展字段可以安全写入 `STARTPAGE_KV`，同时继续保留 `ADMIN_TOKEN` 写入保护。
